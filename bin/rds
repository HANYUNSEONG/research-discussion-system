#!/usr/bin/env sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
CLI="$ROOT/dist/cli.js"

if [ ! -f "$CLI" ]; then
  echo "Missing RDS backend: $CLI" >&2
  echo "Run npm run build in the RDS repository, or install the packaged RDS plugin." >&2
  exit 1
fi

exec node "$CLI" "$@"
