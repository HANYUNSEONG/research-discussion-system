# RDS Core

`core/` defines the provider-neutral file protocol for RDS. These documents should not assume Claude, Codex, Gemini, or Obsidian as the source of truth.

The source of truth is the local RDS Project directory.
