---
name: resume
description: Claude namespace alias for RDS resume. Loads durable context and relevant prior discussions.
---

# RDS Resume Alias

This skill exists so Claude plugin users can invoke `/rds:resume`.

Follow the full workflow in `skills/rds-resume/SKILL.md` with the same arguments.

Task: $ARGUMENTS
